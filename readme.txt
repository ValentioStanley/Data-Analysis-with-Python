1. Hanya install2 library yang ada di notebook Files saja
library yang digunakan:
%pip install pandas
%pip install numpy
%pip install scipy
%pip install matplotlib
%pip install seaborn
%pip install babel
2. Setelah ini otomatis library ke python environment pada local
3. Tulis di terminal command "streamlit run dashboard.py"